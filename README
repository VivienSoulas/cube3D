minimap vectors wall collision
mouse to check at school
anoimation
textue while walking
norminette

remove ./ in front of textures paths (and maybe add .xpm)




parsing think about:
empty map --- > no error message
only new line --- > no error message
only spaces --- > no error message
only tabs --- > no error message

wrong textures path --- > good
different order of inputs --- > good
wrong file name --- > good

double parameters --- > no error

Fixed:
no textures --- > no error